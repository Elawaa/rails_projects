<h1>this is the total data</h1>
<p><%= link_to "Create New Article", new_article_path %></p>
<table>
  <tr>
    <th>
      title
    </th>
    <th>
      description
    </th>
  </tr>
<% @articles.each do |article| %>
<tr>
  <td>
  <%= article.title %>
  </td>
  <td>
  <%= article.description %>
  </td>
  <td>
    <%= link_to 'Edit', edit_article_path(article) %>
    <%= link_to 'Show', article_path(article) %>
  </td>

</tr>
<% end %>
</table>